[package]
name = "pomodoro-rust"
version = "0.1.0"
edition = "2024"
description = "TUI pomodoro para Rust"

[dependencies]
ratatui = "0.30"
crossterm = "0.29"
notify-rust = {version = "4", features = ["d"]}
serde = {version = "1", features = ["derive"]}
toml = "0.8"
dirs = "5"

